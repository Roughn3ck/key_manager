# Forge Prompt — ColdStack v5.3.1 Line: Cosmetic String Cleanup

> **Project:** ColdStack (`/mnt/b/Blockchain/coldstack/`)
> **Version:** stays v5.3.1 — **NO version bump, NO rebuild, NO release**
> **Task:** Fix stale version strings found in the v5.3.1 review; commit + push as a chore
> **Type:** Post-release chore commit. The shipped v5.3.1 EXE and GitHub release stay exactly as they are.

---

## Context

The v5.3.1 review found three groups of stale "v5.3.0" strings that survived the version bump. None affect functionality; one is user-visible in the shipped USB package (launch.bat console banner). This chore fixes the source and the loose bat file. The railgun rewording lands in the **next** build's EXE — the already-shipped v5.3.1 binary keeps its current strings, which is fine and documented here.

**Do NOT:** rebuild the EXE, bump VERSION, touch any GitHub release, touch `gui_main_v5.py`, or change any behavior. String edits only.

---

## Task

### 0. Pre-flight

1. `git status` — tree must be clean (this chore doc is the only expected untracked file). Anything else dirty: STOP and flag.
2. Confirm `git log --oneline -1` shows `e1f113d v5.3.1: Orca Token-2022 close position fix...` and the tag/release `v5.3.1` exists on GitHub.

### 1. Fix `build_gui_v5.py` — five stale version strings

- Line ~3: `PyInstaller build script for ColdStack GUI v5.3.0.` → `... GUI v5.3.1.`
- Line ~6: `Version: v5.3.0 (August 2026) - ColdTrack + Orca Whirlpool + Railgun + Aerodrome + BSC V3` → `Version: v5.3.1 (August 2026) - ColdTrack + Orca Whirlpool + Railgun + Aerodrome + BSC V3`
- Line ~14 (changelog note): `Version strings updated to v5.3.0` → `Version strings updated to v5.3.1 (post-release chore: launch banner, build script strings, railgun stub wording)`
- Line ~215 (launch.bat template): `echo Starting ColdStack v5.3.0...` → `echo Starting ColdStack v5.3.1...`
- Line ~274 (builder console print): `print("ColdStack v5.3.0 - Portable EXE Builder")` → `print("ColdStack v5.3.1 - Portable EXE Builder")`

### 2. Fix `USB_DEPLOYMENT/launch.bat` — direct text edit (no rebuild)

Line 7: `echo Starting ColdStack v5.3.0...` → `echo Starting ColdStack v5.3.1...`

Do NOT run the build script to regenerate it — a direct edit is intentional here so the shipped USB package is correct without rebuilding the EXE. The fixed `build_gui_v5.py` template (step 1) keeps future builds consistent.

### 3. Reword `src/railgun_tab.py` — three stale "coming in v5.3.0" stubs

The Shield/Unshield/Transfer transaction dialogs are still stubs, but v5.3.0 shipped without them — the version promise is stale. Reword to version-neutral "coming soon":

- Line ~617: `self.gui.show_notification("Shield dialog — coming in v5.3.0")` → `self.gui.show_notification("Shield dialog — coming soon")`
- Line ~621: `self.gui.show_notification("Unshield dialog — coming in v5.3.0")` → `self.gui.show_notification("Unshield dialog — coming soon")`
- Line ~625: `self.gui.show_notification("Transfer dialog — coming in v5.3.0")` → `self.gui.show_notification("Transfer dialog — coming soon")`

No other changes to railgun_tab.py — logic untouched, notification style unchanged.

**Note:** this lands in source only; it appears in the next build's EXE (v5.3.2+). The shipped v5.3.1 binary keeps the old strings — acceptable, do not rebuild for this.

### 4. Syntax check

```bash
python -m py_compile build_gui_v5.py src/railgun_tab.py
grep -rn "5\.3\.0" build_gui_v5.py src/railgun_tab.py USB_DEPLOYMENT/launch.bat
```

Compile passes and the grep returns zero matches.

### 5. Git

```bash
git add -A
git status   # expect: build_gui_v5.py, USB_DEPLOYMENT/launch.bat, src/railgun_tab.py, this chore doc
git commit -m "chore: version string cleanup — launch banner, build script, railgun stub wording"
git push origin master
```

### 6. Explicitly out of scope

- No EXE rebuild, no VERSION change, no STATUS/README/AGENTS version edits
- No GitHub release changes (v5.3.1 and its asset remain untouched)
- Historical comments in `gui_main_v5.py` ("# v5.3.0: ColdTrack tab...") are correct — leave them
- `README.md` / `USB_DEPLOYMENT/README.md` "ColdTrack (v5.3.0+)" tags are the module's intro version — leave them

## Verification Checklist

- [ ] Tree was clean before edits; HEAD was the v5.3.1 commit
- [ ] build_gui_v5.py: all five strings now v5.3.1
- [ ] launch.bat banner reads v5.3.1 (direct edit, no rebuild)
- [ ] railgun_tab.py: three stubs say "coming soon", logic untouched
- [ ] py_compile passes; grep for "5.3.0" in the three files returns nothing
- [ ] Committed + pushed to master; no release/version changes anywhere